{
  "name": "@wavex-os/auth-shim",
  "version": "0.1.0",
  "description": "Wavex auth gates that match op-omega's authz surface (assertBoard, assertCompanyAccess, etc.). Switches between dev bypass (synthetic local_implicit board) and Better-Auth via WAVEX_AUTH_MODE.",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "typescript": "^5.5.4",
    "vitest": "^2.1.8"
  }
}
